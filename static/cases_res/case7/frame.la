from trigonometry: sin, cos

M = [`S_v`₁ `S_v`₂
     `S_v`₂ `S_v`₃][cos(theta) -sin(theta)
                    sin(theta) cos(theta)]
v = [(M₁,₁ , M₂,₁)ᵀTP (M₁,₂ , M₂,₂)ᵀTP]

where

`S_v` ∈ ℝ^3
theta ∈ ℝ 
TP ∈ ℝ^(2×3)